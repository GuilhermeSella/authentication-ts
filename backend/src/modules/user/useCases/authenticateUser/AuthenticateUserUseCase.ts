import { prisma } from "../../../../shared/prisma";


class AuthenticateUserUseCase {

    execute(email: string, password: string) {

        const existUser = prisma

    }

}

export default AuthenticateUserUseCase;